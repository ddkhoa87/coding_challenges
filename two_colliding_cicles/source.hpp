#pragma once

/**
 * 6 types of colissions:
 *  + separate: d centers > sum radii
 *  + external tangent: d centers == sum radii
 *  + intersecting: d radii < d centers < sum radii
 *  + internal tangent: d radii == d centers
 *  + inside: d centers < d radii
 *  + identical: d centers == 0 && radii identical
 */
void foo() {
    
}
