#include <iostream>

#include "source.hpp"

int main() {
    std::cout << exp(2, 0) << std::endl;
    std::cout << exp(2, 1) << std::endl;
    std::cout << exp(2, 5) << std::endl;
    std::cout << exp(3, 4) << std::endl;

    return 0;
}